 sum(1,2,3)

 function sum(){
   console.log(arguments)
 }

 /*
 
In JavaScript, the arguments property is an array-like object that contains all the arguments passed to a function. It provides a way to access the arguments dynamically within the function, regardless of the number of arguments defined in the function's signature. 
  * */
