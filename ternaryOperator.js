// ternary opreator on variables
// const happy = true;

// happy === true ? console.log("yea") : console.log("nah");

// ternary opreator on functions

// const checkHappy = (happy) => {
//   return happy === true ? "yea" : "nah";
// };

// checkHappy(false);

//the above code can also be written as

// const checkEmotion = (happy) => (happy === true ? "yea" : "nah");

// const checkResult = checkEmotion(true);

// console.log(checkResult);

// ternary opreator on arrays

// const array = [2, 4, 6, 8, 10, 12, 14];

// const mapped = array.map((num) => (num > 8 ? num : "under"));

// console.log(mapped);
