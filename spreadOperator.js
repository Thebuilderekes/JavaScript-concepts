const numbers = [1, 2, 3, 4, 5, 6, 7];

const myArray = [...numbers, 8, 9];

console.log(myArray);

//with spread operator, strings will get spread out to individual characters
const Myname = "Ekeopre";

const spread = [...Myname];
console.log(spread);

//with spread operator, Sets will get converted to array
