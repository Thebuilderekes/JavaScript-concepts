//Generators allow you to pause the execution of a function so that the .next() can be called to execute the code after each yield. Once a genrator yield has been exhaused it stops
// const counter = function* () {
//   yield 1;
//   yield 2;
//   yield 3;
// };

// console.log(counter);

// const counterGenerator = counter();

// console.log(counterGenerator.next()); // {value : 1, done: false}
//console.log(counterGenerator.next());
//console.log(counterGenerator.next());
